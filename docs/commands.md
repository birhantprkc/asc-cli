<!-- GENERATED by `make docs` from `asc --experimental-dump-help`. Do not edit. -->
# Command reference

Every `asc` command and flag, generated from the binary. For how to use a feature, start at the [docs index](README.md).

**Global flags** (accepted by every command, not repeated below):
`--output json|table|markdown` (default `json`) · `--pretty` · `--timeout <30s|2m>`

**Commands:** [apps](#asc-apps) · [init](#asc-init) · [versions](#asc-versions) · [version-localizations](#asc-version-localizations) · [screenshot-sets](#asc-screenshot-sets) · [screenshots](#asc-screenshots) · [app-infos](#asc-app-infos) · [app-info-localizations](#asc-app-info-localizations) · [builds](#asc-builds) · [testflight](#asc-testflight) · [auth](#asc-auth) · [tui](#asc-tui) · [bundle-ids](#asc-bundle-ids) · [certificates](#asc-certificates) · [devices](#asc-devices) · [profiles](#asc-profiles) · [app-preview-sets](#asc-app-preview-sets) · [app-previews](#asc-app-previews) · [iap](#asc-iap) · [iap-equalizations](#asc-iap-equalizations) · [iap-localizations](#asc-iap-localizations) · [iap-price-schedule](#asc-iap-price-schedule) · [subscription-groups](#asc-subscription-groups) · [subscriptions](#asc-subscriptions) · [subscription-localizations](#asc-subscription-localizations) · [subscription-group-localizations](#asc-subscription-group-localizations) · [subscription-price-schedule](#asc-subscription-price-schedule) · [subscription-equalizations](#asc-subscription-equalizations) · [subscription-offers](#asc-subscription-offers) · [subscription-promotional-offers](#asc-subscription-promotional-offers) · [win-back-offers](#asc-win-back-offers) · [promoted-purchases](#asc-promoted-purchases) · [experiments](#asc-experiments) · [experiment-treatments](#asc-experiment-treatments) · [experiment-treatment-localizations](#asc-experiment-treatment-localizations) · [iap-review-screenshot](#asc-iap-review-screenshot) · [iap-images](#asc-iap-images) · [subscription-review-screenshot](#asc-subscription-review-screenshot) · [subscription-images](#asc-subscription-images) · [subscription-offer-codes](#asc-subscription-offer-codes) · [subscription-offer-code-custom-codes](#asc-subscription-offer-code-custom-codes) · [subscription-offer-code-one-time-codes](#asc-subscription-offer-code-one-time-codes) · [iap-offer-codes](#asc-iap-offer-codes) · [iap-offer-code-custom-codes](#asc-iap-offer-code-custom-codes) · [iap-offer-code-one-time-codes](#asc-iap-offer-code-one-time-codes) · [app-shots](#asc-app-shots) · [age-rating](#asc-age-rating) · [app-categories](#asc-app-categories) · [version-review-detail](#asc-version-review-detail) · [plugins](#asc-plugins) · [skills](#asc-skills) · [app-wall](#asc-app-wall) · [users](#asc-users) · [user-invitations](#asc-user-invitations) · [xcode-cloud](#asc-xcode-cloud) · [game-center](#asc-game-center) · [app-clips](#asc-app-clips) · [app-clip-experiences](#asc-app-clip-experiences) · [app-clip-experience-localizations](#asc-app-clip-experience-localizations) · [sales-reports](#asc-sales-reports) · [finance-reports](#asc-finance-reports) · [analytics-reports](#asc-analytics-reports) · [reviews](#asc-reviews) · [review-responses](#asc-review-responses) · [perf-metrics](#asc-perf-metrics) · [diagnostics](#asc-diagnostics) · [diagnostic-logs](#asc-diagnostic-logs) · [beta-review](#asc-beta-review) · [beta-app-localizations](#asc-beta-app-localizations) · [review-submissions](#asc-review-submissions) · [app-availability](#asc-app-availability) · [iap-availability](#asc-iap-availability) · [subscription-availability](#asc-subscription-availability) · [territories](#asc-territories) · [web-server](#asc-web-server) · [iris](#asc-iris) · [simulators](#asc-simulators)

## asc apps

Manage apps

### asc apps list

List all apps

```
asc apps list [--limit <limit>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--limit` |  |  | Maximum number of apps to return |

### asc apps update

Update app-level metadata (currently: content rights declaration)

```
asc apps update --app-id <app-id> [--content-rights-declaration <content-rights-declaration>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID |
| `--content-rights-declaration` |  |  | Content rights declaration: USES_THIRD_PARTY_CONTENT or DOES_NOT_USE_THIRD_PARTY_CONTENT |

### asc apps price-points list

List the prices an app can be sold at in a territory (the 0 price makes it free)

```
asc apps price-points list --app-id <app-id> [--territory <territory>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID |
| `--territory` |  | `USA` | Territory code (default: USA) |

### asc apps prices set

Set an app's price from a base-territory price point; Apple equalizes the other territories

```
asc apps prices set --app-id <app-id> --base-territory <base-territory> --price-point-id <price-point-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID |
| `--base-territory` | yes |  | Base territory code, e.g. USA |
| `--price-point-id` | yes |  | Price point ID in the base territory (from `asc apps price-points list`) |

## asc init

Initialise project context — saves app ID to .asc/project.json

```
asc init [--app-id <app-id>] [--name <name>] [--contact-first-name <contact-first-name>] [--contact-last-name <contact-last-name>] [--contact-phone <contact-phone>] [--contact-email <contact-email>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` |  |  | App Store Connect app ID |
| `--name` |  |  | App name to search for |
| `--contact-first-name` |  |  | Review contact first name |
| `--contact-last-name` |  |  | Review contact last name |
| `--contact-phone` |  |  | Review contact phone number |
| `--contact-email` |  |  | Review contact email address |

## asc versions

Manage App Store versions

### asc versions list

List App Store versions for an app (one per platform: iOS, macOS, tvOS, …)

```
asc versions list --app-id <app-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID |

### asc versions create

Create a new App Store version

```
asc versions create --app-id <app-id> --platform <platform>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID |
| `--platform` | yes |  | Platform: ios, macos, tvos, watchos, visionos |

### asc versions update

Update an App Store version's version string, copyright, or release schedule

```
asc versions update --version-id <version-id> [--copyright <copyright>] [--release-type <release-type>] [--earliest-release-date <earliest-release-date>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--version-id` | yes |  | App Store version ID |
| `--copyright` |  |  | Copyright line shown on the App Store page (e.g. "© 2026 Acme") |
| `--release-type` |  |  | Release type: MANUAL, AFTER_APPROVAL, or SCHEDULED |
| `--earliest-release-date` |  |  | ISO-8601 timestamp for SCHEDULED releases (e.g. 2026-06-01T00:00:00Z) |

### asc versions submit

Submit an App Store version for review

```
asc versions submit --version-id <version-id> [--with-products] [--dry-run]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--version-id` | yes |  | App Store Version ID |
| `--with-products` |  |  | Also submit every ready in-app purchase and subscription version with this app version |
| `--dry-run` |  |  | List what would be submitted without submitting (implies --with-products) |

### asc versions set-build

Link a build to an App Store version before submission

```
asc versions set-build --version-id <version-id> --build-id <build-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--version-id` | yes |  | App Store version ID |
| `--build-id` | yes |  | Build ID |

### asc versions check-readiness

Pre-flight check for App Store submission readiness

```
asc versions check-readiness --version-id <version-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--version-id` | yes |  | App Store Version ID |

## asc version-localizations

Manage App Store version localizations

### asc version-localizations list

List localizations for an App Store version

```
asc version-localizations list --version-id <version-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--version-id` | yes |  | App Store version ID |

### asc version-localizations create

Create a new App Store version localization

```
asc version-localizations create --version-id <version-id> --locale <locale>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--version-id` | yes |  | App Store version ID |
| `--locale` | yes |  | Locale identifier (e.g. en-US, zh-Hans) |

### asc version-localizations update

Update text content for an App Store version localization

```
asc version-localizations update --localization-id <localization-id> [--whats-new <whats-new>] [--description <description>] [--keywords <keywords>] [--marketing-url <marketing-url>] [--support-url <support-url>] [--promotional-text <promotional-text>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--localization-id` | yes |  | App Store version localization ID |
| `--whats-new` |  |  | What's New text (release notes) |
| `--description` |  |  | App description |
| `--keywords` |  |  | Keywords (comma-separated) |
| `--marketing-url` |  |  | Marketing URL |
| `--support-url` |  |  | Support URL |
| `--promotional-text` |  |  | Promotional text |

## asc screenshot-sets

Manage App Store screenshot sets

### asc screenshot-sets list

List screenshot sets for an App Store version localization

```
asc screenshot-sets list --localization-id <localization-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--localization-id` | yes |  | App Store version localization ID |

### asc screenshot-sets create

Create a new screenshot set for a localization

```
asc screenshot-sets create --localization-id <localization-id> --display-type <display-type>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--localization-id` | yes |  | Localization ID |
| `--display-type` | yes |  | Screenshot display type (e.g. APP_IPHONE_67) |

## asc screenshots

Manage App Store screenshots

### asc screenshots list

List screenshots in a screenshot set

```
asc screenshots list --set-id <set-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--set-id` | yes |  | Screenshot set ID |

### asc screenshots upload

Upload a screenshot to a screenshot set

```
asc screenshots upload --set-id <set-id> --file <file>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--set-id` | yes |  | Screenshot set ID |
| `--file` | yes |  | Path to the image file |

### asc screenshots import

Import screenshots from an exported ZIP file

```
asc screenshots import --version-id <version-id> --from <from>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--version-id` | yes |  | App Store version ID |
| `--from` | yes |  | Path to export.zip from the screenshot editor |

## asc app-infos

Manage App Store app info

### asc app-infos list

List app infos for an app

```
asc app-infos list --app-id <app-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID |

### asc app-infos update

Update app info categories

```
asc app-infos update --app-info-id <app-info-id> [--primary-category <primary-category>] [--primary-subcategory-one <primary-subcategory-one>] [--primary-subcategory-two <primary-subcategory-two>] [--secondary-category <secondary-category>] [--secondary-subcategory-one <secondary-subcategory-one>] [--secondary-subcategory-two <secondary-subcategory-two>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-info-id` | yes |  | App info ID |
| `--primary-category` |  |  | Primary category ID (e.g. 6014 for Games) |
| `--primary-subcategory-one` |  |  | Primary subcategory 1 ID |
| `--primary-subcategory-two` |  |  | Primary subcategory 2 ID |
| `--secondary-category` |  |  | Secondary category ID |
| `--secondary-subcategory-one` |  |  | Secondary subcategory 1 ID |
| `--secondary-subcategory-two` |  |  | Secondary subcategory 2 ID |

## asc app-info-localizations

Manage App Store app info localizations

### asc app-info-localizations list

List localizations for an app info

```
asc app-info-localizations list --app-info-id <app-info-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-info-id` | yes |  | App info ID |

### asc app-info-localizations create

Create a new app info localization

```
asc app-info-localizations create --app-info-id <app-info-id> --locale <locale> --name <name>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-info-id` | yes |  | App info ID |
| `--locale` | yes |  | Locale identifier (e.g. en-US, zh-Hans) |
| `--name` | yes |  | App name (up to 30 characters) |

### asc app-info-localizations update

Update an app info localization

```
asc app-info-localizations update --localization-id <localization-id> [--name <name>] [--subtitle <subtitle>] [--privacy-policy-url <privacy-policy-url>] [--privacy-choices-url <privacy-choices-url>] [--privacy-policy-text <privacy-policy-text>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--localization-id` | yes |  | Localization ID |
| `--name` |  |  | App name (up to 30 characters) |
| `--subtitle` |  |  | Subtitle (up to 30 characters) |
| `--privacy-policy-url` |  |  | Privacy policy URL |
| `--privacy-choices-url` |  |  | Privacy choices URL |
| `--privacy-policy-text` |  |  | Privacy policy text |

### asc app-info-localizations delete

Delete an app info localization

```
asc app-info-localizations delete --localization-id <localization-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--localization-id` | yes |  | Localization ID |

## asc builds

Manage builds

### asc builds list

List builds

```
asc builds list [--app-id <app-id>] [--platform <platform>] [--limit <limit>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` |  |  | Filter by app ID |
| `--platform` |  |  | Filter by platform (ios, macos, tvos, visionos) |
| `--limit` |  |  | Maximum number of builds to return |

### asc builds next-number

Get the next build number for a version

```
asc builds next-number --app-id <app-id> --platform <platform>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID (required) |
| `--platform` | yes |  | Platform (ios, macos, tvos, visionos) |

### asc builds upload

Upload an IPA or PKG to App Store Connect

```
asc builds upload --app-id <app-id> --file <file> --build-number <build-number> [--platform <platform>] [--wait]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID |
| `--file` | yes |  | Path to .ipa (iOS/tvOS/visionOS) or .pkg (macOS) file |
| `--build-number` | yes |  | CFBundleVersion build number (e.g. 42) |
| `--platform` |  |  | Platform: ios, macos, tvos, visionos (auto-detected from file extension) |
| `--wait` |  |  | Poll until build processing completes |

### asc builds archive

Archive and export an Xcode project for App Store / TestFlight

```
asc builds archive --scheme <scheme> [--workspace <workspace>] [--project <project>] [--platform <platform>] [--configuration <configuration>] [--export-method <export-method>] [--signing-style <signing-style>] [--team-id <team-id>] [--output-dir <output-dir>] [--upload] [--app-id <app-id>] [--build-number <build-number>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--scheme` | yes |  | Xcode scheme to archive |
| `--workspace` |  |  | Path to .xcworkspace (auto-detected if omitted) |
| `--project` |  |  | Path to .xcodeproj (auto-detected if omitted) |
| `--platform` |  |  | Platform: ios, macos, tvos, visionos (default: ios) |
| `--configuration` |  | `Release` | Build configuration (default: Release) |
| `--export-method` |  |  | Export method: app-store-connect, ad-hoc, development, enterprise (default: app-store-connect) |
| `--signing-style` |  |  | Signing style: automatic, manual (default: automatic) |
| `--team-id` |  |  | Team ID for signing |
| `--output-dir` |  | `.build` | Output directory for archive and export (default: .build) |
| `--upload` |  |  | Upload the exported IPA/PKG to App Store Connect after archiving |
| `--app-id` |  |  | App ID (required if --upload) |
| `--build-number` |  |  | Build number (required if --upload, e.g. 42) |

### asc builds uploads list

List build upload records for an app

```
asc builds uploads list --app-id <app-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID |

### asc builds uploads get

Get a build upload record by ID

```
asc builds uploads get --upload-id <upload-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--upload-id` | yes |  | Build upload ID |

### asc builds uploads delete

Delete a build upload record

```
asc builds uploads delete --upload-id <upload-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--upload-id` | yes |  | Build upload ID |

### asc builds add-beta-group

Add a beta group to a build for TestFlight distribution

```
asc builds add-beta-group --build-id <build-id> --beta-group-id <beta-group-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--build-id` | yes |  | Build ID |
| `--beta-group-id` | yes |  | Beta group ID |

### asc builds remove-beta-group

Remove a beta group from a build

```
asc builds remove-beta-group --build-id <build-id> --beta-group-id <beta-group-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--build-id` | yes |  | Build ID |
| `--beta-group-id` | yes |  | Beta group ID |

### asc builds update-beta-notes

Set TestFlight 'What's New' notes for a build locale

```
asc builds update-beta-notes --build-id <build-id> --locale <locale> --notes <notes>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--build-id` | yes |  | Build ID |
| `--locale` | yes |  | Locale (e.g. en-US) |
| `--notes` | yes |  | What's new notes text |

### asc builds set-encryption-compliance

Set Apple's export-compliance answer (Info.plist ITSAppUsesNonExemptEncryption) on a build

```
asc builds set-encryption-compliance --build-id <build-id> --uses-non-exempt-encryption <uses-non-exempt-encryption>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--build-id` | yes |  | Build ID |
| `--uses-non-exempt-encryption` | yes |  | Whether the build uses non-exempt encryption (true\|false) |

## asc testflight

Manage TestFlight beta testing

### asc testflight groups list

List beta groups

```
asc testflight groups list [--app-id <app-id>] [--limit <limit>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` |  |  | Filter by app ID |
| `--limit` |  |  | Maximum number of groups to return |

### asc testflight groups create

Create a beta group (external by default; use --internal for an internal group)

```
asc testflight groups create --app-id <app-id> --name <name> [--internal] [--public-link-enabled] [--no-public-link-enabled] [--feedback-enabled] [--no-feedback-enabled]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID that owns the group |
| `--name` | yes |  | Group name |
| `--internal` |  |  | Create an internal beta group (testers must be members of your App Store Connect team). Default: external. |
| `--public-link-enabled` |  | `--no-public-link-enabled` | Enable the public TestFlight link (external groups only) |
| `--no-public-link-enabled` |  |  | Enable the public TestFlight link (external groups only) |
| `--feedback-enabled` |  | `--no-feedback-enabled` | Enable tester feedback (external groups only) |
| `--no-feedback-enabled` |  |  | Enable tester feedback (external groups only) |

### asc testflight testers list

List beta testers in a group

```
asc testflight testers list --beta-group-id <beta-group-id> [--limit <limit>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--beta-group-id` | yes |  | Beta group ID |
| `--limit` |  |  | Maximum number of testers to return |

### asc testflight testers add

Add a beta tester to a group

```
asc testflight testers add --beta-group-id <beta-group-id> --email <email> [--first-name <first-name>] [--last-name <last-name>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--beta-group-id` | yes |  | Beta group ID |
| `--email` | yes |  | Tester email address |
| `--first-name` |  |  | Tester first name |
| `--last-name` |  |  | Tester last name |

### asc testflight testers remove

Remove a beta tester from a group

```
asc testflight testers remove --beta-group-id <beta-group-id> --tester-id <tester-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--beta-group-id` | yes |  | Beta group ID |
| `--tester-id` | yes |  | Tester ID to remove |

### asc testflight testers import

Import beta testers from a CSV file (columns: email,firstName,lastName)

```
asc testflight testers import --beta-group-id <beta-group-id> --file <file>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--beta-group-id` | yes |  | Beta group ID |
| `--file` | yes |  | Path to CSV file with columns: email,firstName,lastName |

### asc testflight testers export

Export beta testers from a group as CSV

```
asc testflight testers export --beta-group-id <beta-group-id> [--limit <limit>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--beta-group-id` | yes |  | Beta group ID |
| `--limit` |  |  | Maximum number of testers to export |

## asc auth

Manage App Store Connect authentication

### asc auth check

Verify authentication credentials and show status

```
asc auth check
```

### asc auth login

Save API key credentials for authentication

```
asc auth login --key-id <key-id> --issuer-id <issuer-id> [--name <name>] [--private-key-path <private-key-path>] [--private-key <private-key>] [--vendor-number <vendor-number>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--key-id` | yes |  | App Store Connect API Key ID |
| `--issuer-id` | yes |  | App Store Connect Issuer ID |
| `--name` |  |  | Account name (defaults to "default"); no spaces allowed |
| `--private-key-path` |  |  | Path to .p8 private key file |
| `--private-key` |  |  | Private key PEM content (alternative to --private-key-path) |
| `--vendor-number` |  |  | Vendor number for financial/sales reports (found in App Store Connect → Payments and Financial Reports) |

### asc auth logout

Remove saved authentication credentials

```
asc auth logout [--name <name>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--name` |  |  | Account name to remove (defaults to active account) |

### asc auth list

List all saved App Store Connect accounts

```
asc auth list
```

### asc auth use

Switch the active App Store Connect account

```
asc auth use <name>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `<name>` | yes |  | Account name to activate |

### asc auth update

Update an existing account (e.g. add vendor number)

```
asc auth update [--name <name>] [--vendor-number <vendor-number>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--name` |  |  | Account name (defaults to active account) |
| `--vendor-number` |  |  | Vendor number for financial/sales reports |

## asc tui

Interactive terminal UI

```
asc tui
```

## asc bundle-ids

Manage bundle identifiers

### asc bundle-ids list

List bundle identifiers

```
asc bundle-ids list [--platform <platform>] [--identifier <identifier>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--platform` |  |  | Filter by platform (ios, macos, universal) |
| `--identifier` |  |  | Filter by bundle identifier string (e.g. com.example.app) |

### asc bundle-ids create

Register a new bundle identifier

```
asc bundle-ids create --name <name> --identifier <identifier> --platform <platform>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--name` | yes |  | Display name for the bundle ID |
| `--identifier` | yes |  | Bundle identifier string (e.g. com.example.app) |
| `--platform` | yes |  | Platform: ios, macos, or universal |

### asc bundle-ids delete

Delete a bundle identifier

```
asc bundle-ids delete --bundle-id-id <bundle-id-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--bundle-id-id` | yes |  | Bundle ID resource ID |

## asc certificates

Manage signing certificates

### asc certificates list

List signing certificates

```
asc certificates list [--type <type>] [--limit <limit>] [--expired-only] [--before <before>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--type` |  |  | Filter by certificate type (e.g. IOS_DISTRIBUTION, MAC_APP_STORE) |
| `--limit` |  |  | Maximum number of certificates to return (server-side) |
| `--expired-only` |  |  | Only return certificates whose expirationDate has passed |
| `--before` |  |  | Only return certificates with expirationDate strictly before this date. Accepts `YYYY-MM-DD` (midnight UTC) or full ISO8601 (`2026-11-01T00:00:00Z`) |

### asc certificates create

Create a signing certificate from a CSR

```
asc certificates create --type <type> [--csr-path <csr-path>] [--csr-content <csr-content>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--type` | yes |  | Certificate type (e.g. IOS_DISTRIBUTION, MAC_APP_DISTRIBUTION) |
| `--csr-path` |  |  | Path to the .csr file — use this for PEM files (avoids shell quoting issues with dashes) |
| `--csr-content` |  |  | CSR content as a string — NOTE: PEM files start with '-----' which breaks shell argument parsing; use --csr-path instead |

### asc certificates revoke

Revoke a signing certificate

```
asc certificates revoke --certificate-id <certificate-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--certificate-id` | yes |  | Certificate resource ID |

## asc devices

Manage registered test devices

### asc devices list

List registered test devices

```
asc devices list [--platform <platform>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--platform` |  |  | Filter by platform (ios, macos) |

### asc devices register

Register a test device

```
asc devices register --name <name> --udid <udid> --platform <platform>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--name` | yes |  | Device name |
| `--udid` | yes |  | Device UDID |
| `--platform` | yes |  | Platform: ios or macos |

## asc profiles

Manage provisioning profiles

### asc profiles list

List provisioning profiles

```
asc profiles list [--bundle-id-id <bundle-id-id>] [--type <type>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--bundle-id-id` |  |  | Filter by bundle ID resource ID |
| `--type` |  |  | Filter by profile type (e.g. IOS_APP_STORE, MAC_APP_STORE) |

### asc profiles create

Create a provisioning profile

```
asc profiles create --name <name> --type <type> --bundle-id-id <bundle-id-id> --certificate-ids <certificate-ids> [--device-ids <device-ids>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--name` | yes |  | Profile name |
| `--type` | yes |  | Profile type (e.g. IOS_APP_STORE, MAC_APP_STORE) |
| `--bundle-id-id` | yes |  | Bundle ID resource ID |
| `--certificate-ids` | yes |  | Comma-separated certificate resource IDs |
| `--device-ids` |  |  | Comma-separated device resource IDs (optional, for development profiles) |

### asc profiles delete

Delete a provisioning profile

```
asc profiles delete --profile-id <profile-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--profile-id` | yes |  | Profile resource ID |

## asc app-preview-sets

Manage App Store app preview sets

### asc app-preview-sets list

List app preview sets for an App Store version localization

```
asc app-preview-sets list --localization-id <localization-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--localization-id` | yes |  | App Store version localization ID |

### asc app-preview-sets create

Create an app preview set for a localization

```
asc app-preview-sets create --localization-id <localization-id> --preview-type <preview-type>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--localization-id` | yes |  | App Store version localization ID |
| `--preview-type` | yes |  | Preview type raw value (e.g. IPHONE_67, IPAD_PRO_3GEN_129, APPLE_TV) |

## asc app-previews

Manage App Store app preview videos

### asc app-previews list

List app previews in a preview set

```
asc app-previews list --set-id <set-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--set-id` | yes |  | App preview set ID |

### asc app-previews upload

Upload a video file to an app preview set

```
asc app-previews upload --set-id <set-id> --file <file> [--preview-frame-time-code <preview-frame-time-code>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--set-id` | yes |  | App preview set ID |
| `--file` | yes |  | Path to video file (.mp4, .mov, .m4v) |
| `--preview-frame-time-code` |  |  | Preview frame timecode (e.g. 00:00:05) |

## asc iap

Manage in-app purchases

### asc iap list

List in-app purchases for an app

```
asc iap list --app-id <app-id> [--limit <limit>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID to list IAPs for |
| `--limit` |  |  | Maximum number of results |

### asc iap create

Create a new in-app purchase

```
asc iap create --app-id <app-id> --reference-name <reference-name> --product-id <product-id> --type <type>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID to create the IAP for |
| `--reference-name` | yes |  | Internal reference name (not displayed to users) |
| `--product-id` | yes |  | Product ID (e.g. com.app.goldcoins) |
| `--type` | yes |  | IAP type: consumable, non-consumable, or non-renewing-subscription |

### asc iap update

Update an in-app purchase (reference name, review note, family sharable)

```
asc iap update --iap-id <iap-id> [--reference-name <reference-name>] [--review-note <review-note>] [--family-sharable] [--not-family-sharable]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--iap-id` | yes |  | IAP ID |
| `--reference-name` |  |  | New reference name |
| `--review-note` |  |  | App Review note |
| `--family-sharable` |  |  | Mark as Family Sharable |
| `--not-family-sharable` |  |  | Mark as not Family Sharable |

### asc iap delete

Delete an in-app purchase

```
asc iap delete --iap-id <iap-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--iap-id` | yes |  | IAP ID |

### asc iap submit

Submit an in-app purchase for App Store review

```
asc iap submit --iap-id <iap-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--iap-id` | yes |  | IAP ID to submit for review |

### asc iap unsubmit

Withdraw an in-app purchase from review by deleting its submission

```
asc iap unsubmit --submission-id <submission-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--submission-id` | yes |  | Submission ID |

### asc iap price-points list

List available price points for an in-app purchase

```
asc iap price-points list --iap-id <iap-id> [--territory <territory>] [--limit <limit>] [--cursor <cursor>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--iap-id` | yes |  | IAP ID |
| `--territory` |  |  | Filter by territory code (e.g. USA) |
| `--limit` |  |  | Page size (default: ASC's ~50) |
| `--cursor` |  |  | Cursor from a previous response's `nextCursor` to fetch the next page |

### asc iap prices set

Set the price schedule for an in-app purchase

```
asc iap prices set --iap-id <iap-id> --base-territory <base-territory> --price-point-id <price-point-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--iap-id` | yes |  | IAP ID |
| `--base-territory` | yes |  | Base territory for pricing (e.g. USA) |
| `--price-point-id` | yes |  | Price point ID from asc iap price-points list |

### asc iap versions list

List review versions of an in-app purchase (add a submittable one to a review submission)

```
asc iap versions list --iap-id <iap-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--iap-id` | yes |  | IAP ID |

## asc iap-equalizations

List Apple's auto-equalized territory prices for a given IAP price point

### asc iap-equalizations list

List equalized territory prices for an IAP price point

```
asc iap-equalizations list --price-point-id <price-point-id> [--limit <limit>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--price-point-id` | yes |  | IAP price point ID (from `asc iap price-points list`) |
| `--limit` |  |  | Maximum number of entries to return (default 200) |

## asc iap-localizations

Manage in-app purchase localizations

### asc iap-localizations list

List localizations for an in-app purchase

```
asc iap-localizations list --iap-id <iap-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--iap-id` | yes |  | IAP ID to list localizations for |

### asc iap-localizations create

Create a localization for an in-app purchase

```
asc iap-localizations create --iap-id <iap-id> --locale <locale> --name <name> [--description <description>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--iap-id` | yes |  | IAP ID to create localization for |
| `--locale` | yes |  | Locale code (e.g. en-US, zh-Hans) |
| `--name` | yes |  | Display name shown to users |
| `--description` |  |  | Optional description shown to users |

### asc iap-localizations update

Update a localization for an in-app purchase

```
asc iap-localizations update --localization-id <localization-id> [--name <name>] [--description <description>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--localization-id` | yes |  | Localization ID |
| `--name` |  |  | New display name |
| `--description` |  |  | New description |

### asc iap-localizations delete

Delete a localization for an in-app purchase

```
asc iap-localizations delete --localization-id <localization-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--localization-id` | yes |  | Localization ID |

## asc iap-price-schedule

Manage the price schedule (currently set price) for an in-app purchase

### asc iap-price-schedule get

Get the current price schedule for an in-app purchase

```
asc iap-price-schedule get --iap-id <iap-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--iap-id` | yes |  | IAP ID |

## asc subscription-groups

Manage subscription groups

### asc subscription-groups list

List subscription groups for an app

```
asc subscription-groups list --app-id <app-id> [--limit <limit>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID to list subscription groups for |
| `--limit` |  |  | Maximum number of results |

### asc subscription-groups create

Create a new subscription group

```
asc subscription-groups create --app-id <app-id> --reference-name <reference-name>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID to create the subscription group for |
| `--reference-name` | yes |  | Internal reference name for the group |

### asc subscription-groups update

Update a subscription group reference name

```
asc subscription-groups update --group-id <group-id> --reference-name <reference-name>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--group-id` | yes |  | Subscription group ID |
| `--reference-name` | yes |  | New reference name |

### asc subscription-groups delete

Delete a subscription group

```
asc subscription-groups delete --group-id <group-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--group-id` | yes |  | Subscription group ID |

### asc subscription-groups versions list

List review versions of a subscription group (add a submittable one to a review submission)

```
asc subscription-groups versions list --group-id <group-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--group-id` | yes |  | Subscription group ID |

## asc subscriptions

Manage subscriptions

### asc subscriptions list

List subscriptions in a subscription group

```
asc subscriptions list --group-id <group-id> [--limit <limit>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--group-id` | yes |  | Subscription group ID |
| `--limit` |  |  | Maximum number of results |

### asc subscriptions create

Create a new subscription in a group

```
asc subscriptions create --group-id <group-id> --name <name> --product-id <product-id> --period <period> [--family-sharable] [--group-level <group-level>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--group-id` | yes |  | Subscription group ID |
| `--name` | yes |  | Display name for the subscription |
| `--product-id` | yes |  | Product ID (e.g. com.app.monthly) |
| `--period` | yes |  | Billing period: ONE_WEEK, ONE_MONTH, TWO_MONTHS, THREE_MONTHS, SIX_MONTHS, ONE_YEAR |
| `--family-sharable` |  |  | Enable Family Sharing for this subscription |
| `--group-level` |  |  | Level within the group (used for upgrade/downgrade) |

### asc subscriptions update

Update a subscription (name, family sharable, group level, review note)

```
asc subscriptions update --subscription-id <subscription-id> [--name <name>] [--family-sharable] [--not-family-sharable] [--group-level <group-level>] [--period <period>] [--review-note <review-note>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID |
| `--name` |  |  | New display name |
| `--family-sharable` |  |  | Mark as Family Sharable |
| `--not-family-sharable` |  |  | Mark as not Family Sharable |
| `--group-level` |  |  | Group level for upgrade/downgrade ordering |
| `--period` |  |  | Billing period (ONE_WEEK, ONE_MONTH, TWO_MONTHS, THREE_MONTHS, SIX_MONTHS, ONE_YEAR) |
| `--review-note` |  |  | App Review note |

### asc subscriptions delete

Delete a subscription

```
asc subscriptions delete --subscription-id <subscription-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID |

### asc subscriptions submit

Submit a subscription for App Store review

```
asc subscriptions submit --subscription-id <subscription-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID to submit for review |

### asc subscriptions unsubmit

Withdraw a subscription from review by deleting its submission

```
asc subscriptions unsubmit --submission-id <submission-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--submission-id` | yes |  | Submission ID |

### asc subscriptions price-points list

List available price points for a subscription

```
asc subscriptions price-points list --subscription-id <subscription-id> [--territory <territory>] [--limit <limit>] [--cursor <cursor>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID |
| `--territory` |  |  | Filter by territory code (e.g. USA) |
| `--limit` |  |  | Page size (default: ASC's ~50) |
| `--cursor` |  |  | Cursor from a previous response's `nextCursor` to fetch the next page |

### asc subscriptions prices set

Set the per-territory price for a subscription

```
asc subscriptions prices set --subscription-id <subscription-id> --territory <territory> --price-point-id <price-point-id> [--start-date <start-date>] [--preserve-current-price]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID |
| `--territory` | yes |  | Territory code (e.g. USA) |
| `--price-point-id` | yes |  | Price point ID from `asc subscriptions price-points list` |
| `--start-date` |  |  | Optional start date YYYY-MM-DD |
| `--preserve-current-price` |  |  | Preserve current price during the transition |

### asc subscriptions prices set-batch

Set multiple per-territory prices in a single call (mirrors iOS `setPrices(prices:)`)

```
asc subscriptions prices set-batch --subscription-id <subscription-id> [--price <price> …] [--start-date <start-date>] [--preserve-current-price]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID |
| `--price` |  |  | Repeatable territory=pricePointId pair, e.g. --price USA=spp-1 --price JPN=spp-2 |
| `--start-date` |  |  | Optional shared start date YYYY-MM-DD applied to every entry |
| `--preserve-current-price` |  |  | Preserve current price during the transition for every entry |

### asc subscriptions versions list

List review versions of a subscription (add a submittable one to a review submission)

```
asc subscriptions versions list --subscription-id <subscription-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID |

## asc subscription-localizations

Manage subscription localizations

### asc subscription-localizations list

List localizations for a subscription

```
asc subscription-localizations list --subscription-id <subscription-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID to list localizations for |

### asc subscription-localizations create

Create a localization for a subscription

```
asc subscription-localizations create --subscription-id <subscription-id> --locale <locale> --name <name> [--description <description>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID to create localization for |
| `--locale` | yes |  | Locale code (e.g. en-US, zh-Hans) |
| `--name` | yes |  | Display name shown to users in the App Store |
| `--description` |  |  | Optional description shown to users |

### asc subscription-localizations update

Update a localization for a subscription

```
asc subscription-localizations update --localization-id <localization-id> [--name <name>] [--description <description>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--localization-id` | yes |  | Localization ID |
| `--name` |  |  | New display name |
| `--description` |  |  | New description |

### asc subscription-localizations delete

Delete a localization for a subscription

```
asc subscription-localizations delete --localization-id <localization-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--localization-id` | yes |  | Localization ID |

## asc subscription-group-localizations

Manage subscription group localizations (display name and custom app name per locale)

### asc subscription-group-localizations list

List localizations for a subscription group

```
asc subscription-group-localizations list --group-id <group-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--group-id` | yes |  | Subscription group ID |

### asc subscription-group-localizations create

Create a localization for a subscription group

```
asc subscription-group-localizations create --group-id <group-id> --locale <locale> --name <name> [--custom-app-name <custom-app-name>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--group-id` | yes |  | Subscription group ID |
| `--locale` | yes |  | Locale code (e.g. en-US, zh-Hans) |
| `--name` | yes |  | Display name shown to users |
| `--custom-app-name` |  |  | Optional custom app name |

### asc subscription-group-localizations update

Update a localization for a subscription group

```
asc subscription-group-localizations update --localization-id <localization-id> [--name <name>] [--custom-app-name <custom-app-name>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--localization-id` | yes |  | Localization ID |
| `--name` |  |  | New display name |
| `--custom-app-name` |  |  | New custom app name |

### asc subscription-group-localizations delete

Delete a subscription group localization

```
asc subscription-group-localizations delete --localization-id <localization-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--localization-id` | yes |  | Localization ID |

## asc subscription-price-schedule

Manage the per-territory price schedule for a subscription

### asc subscription-price-schedule get

Get the per-territory price schedule for a subscription

```
asc subscription-price-schedule get --subscription-id <subscription-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID |

## asc subscription-equalizations

List Apple's auto-equalized territory prices for a given subscription price point

### asc subscription-equalizations list

List equalized territory prices for a subscription price point

```
asc subscription-equalizations list --price-point-id <price-point-id> [--limit <limit>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--price-point-id` | yes |  | Subscription price point ID (from `asc subscriptions price-points list`) |
| `--limit` |  |  | Maximum number of entries to return (default 200) |

## asc subscription-offers

Manage subscription introductory offers

### asc subscription-offers list

List introductory offers for a subscription

```
asc subscription-offers list --subscription-id <subscription-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID |

### asc subscription-offers create

Create an introductory offer for a subscription

```
asc subscription-offers create --subscription-id <subscription-id> --duration <duration> --mode <mode> --periods <periods> [--territory <territory>] [--price-point-id <price-point-id>] [--start-date <start-date>] [--end-date <end-date>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID |
| `--duration` | yes |  | Offer duration: THREE_DAYS, ONE_WEEK, TWO_WEEKS, ONE_MONTH, TWO_MONTHS, THREE_MONTHS, SIX_MONTHS, ONE_YEAR |
| `--mode` | yes |  | Offer mode: PAY_AS_YOU_GO, PAY_UP_FRONT, FREE_TRIAL |
| `--periods` | yes |  | Number of periods |
| `--territory` |  |  | Territory (e.g. USA) — optional |
| `--price-point-id` |  |  | Subscription price point ID — required for PAY_AS_YOU_GO and PAY_UP_FRONT |
| `--start-date` |  |  | Start date in YYYY-MM-DD format — optional |
| `--end-date` |  |  | End date in YYYY-MM-DD format — optional |

### asc subscription-offers delete

Delete an introductory offer

```
asc subscription-offers delete --offer-id <offer-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--offer-id` | yes |  | Introductory offer ID |

## asc subscription-promotional-offers

Manage subscription promotional offers (with per-territory pricing)

### asc subscription-promotional-offers list

List promotional offers for a subscription

```
asc subscription-promotional-offers list --subscription-id <subscription-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID |

### asc subscription-promotional-offers create

Create a promotional offer with per-territory pricing

```
asc subscription-promotional-offers create --subscription-id <subscription-id> --name <name> --offer-code <offer-code> --duration <duration> --mode <mode> --periods <periods> [--price <price> …]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID |
| `--name` | yes |  | Internal name for the offer |
| `--offer-code` | yes |  | Offer code (consumer-facing identifier passed to StoreKit) |
| `--duration` | yes |  | THREE_DAYS, ONE_WEEK, TWO_WEEKS, ONE_MONTH, TWO_MONTHS, THREE_MONTHS, SIX_MONTHS, ONE_YEAR |
| `--mode` | yes |  | FREE_TRIAL, PAY_AS_YOU_GO, PAY_UP_FRONT |
| `--periods` | yes |  | Number of periods |
| `--price` |  |  | Per-territory price specs in TERRITORY=PRICE_POINT_ID form, repeatable (e.g. --price USA=pp-1 --price GBR=pp-2) |

### asc subscription-promotional-offers delete

Delete a promotional offer

```
asc subscription-promotional-offers delete --offer-id <offer-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--offer-id` | yes |  | Promotional offer ID |

### asc subscription-promotional-offers prices list

List per-territory prices for a promotional offer

```
asc subscription-promotional-offers prices list --offer-id <offer-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--offer-id` | yes |  | Promotional offer ID |

## asc win-back-offers

Manage win-back offers for lapsed subscribers

### asc win-back-offers list

List win-back offers for a subscription

```
asc win-back-offers list --subscription-id <subscription-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID |

### asc win-back-offers create

Create a win-back offer with eligibility rules and per-territory pricing

```
asc win-back-offers create --subscription-id <subscription-id> --reference-name <reference-name> --offer-id <offer-id> --duration <duration> --mode <mode> --periods <periods> --paid-months <paid-months> --since-min <since-min> --since-max <since-max> --start-date <start-date> --priority <priority> [--wait-months <wait-months>] [--end-date <end-date>] [--promotion-intent <promotion-intent>] [--price <price> …]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID |
| `--reference-name` | yes |  | Internal name |
| `--offer-id` | yes |  | Offer ID (consumer-facing identifier) |
| `--duration` | yes |  | Duration: THREE_DAYS, ONE_WEEK, TWO_WEEKS, ONE_MONTH, TWO_MONTHS, THREE_MONTHS, SIX_MONTHS, ONE_YEAR |
| `--mode` | yes |  | Mode: FREE_TRIAL, PAY_AS_YOU_GO, PAY_UP_FRONT |
| `--periods` | yes |  | Number of periods |
| `--paid-months` | yes |  | Months the customer must have paid before they qualify |
| `--since-min` | yes |  | Minimum months since last subscribed |
| `--since-max` | yes |  | Maximum months since last subscribed |
| `--wait-months` |  |  | Optional minimum wait between offers in months |
| `--start-date` | yes |  | Start date YYYY-MM-DD |
| `--end-date` |  |  | Optional end date YYYY-MM-DD |
| `--priority` | yes |  | Priority: HIGH or NORMAL |
| `--promotion-intent` |  |  | Optional promotion intent: NOT_PROMOTED or USE_AUTO_GENERATED_ASSETS |
| `--price` |  |  | Per-territory price specs in TERRITORY=PRICE_POINT_ID form, repeatable |

### asc win-back-offers update

Update a win-back offer (priority, dates, eligibility)

```
asc win-back-offers update --offer-id <offer-id> [--start-date <start-date>] [--end-date <end-date>] [--priority <priority>] [--promotion-intent <promotion-intent>] [--paid-months <paid-months>] [--since-min <since-min>] [--since-max <since-max>] [--wait-months <wait-months>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--offer-id` | yes |  | Win-back offer ID |
| `--start-date` |  |  | New start date YYYY-MM-DD |
| `--end-date` |  |  | New end date YYYY-MM-DD |
| `--priority` |  |  | New priority: HIGH or NORMAL |
| `--promotion-intent` |  |  | New promotion intent: NOT_PROMOTED or USE_AUTO_GENERATED_ASSETS |
| `--paid-months` |  |  | Months the customer must have paid |
| `--since-min` |  |  | Minimum months since last subscribed |
| `--since-max` |  |  | Maximum months since last subscribed |
| `--wait-months` |  |  | Wait between offers in months |

### asc win-back-offers delete

Delete a win-back offer

```
asc win-back-offers delete --offer-id <offer-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--offer-id` | yes |  | Win-back offer ID |

### asc win-back-offers prices list

List per-territory prices for a win-back offer

```
asc win-back-offers prices list --offer-id <offer-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--offer-id` | yes |  | Win-back offer ID |

## asc promoted-purchases

Manage promoted in-app purchases (App Store product page slots)

### asc promoted-purchases list

List promoted purchase slots for an app

```
asc promoted-purchases list --app-id <app-id> [--limit <limit>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID |
| `--limit` |  |  | Max results |

### asc promoted-purchases create

Create a promoted purchase slot referencing an IAP or subscription

```
asc promoted-purchases create --app-id <app-id> [--iap-id <iap-id>] [--subscription-id <subscription-id>] [--visible] [--hidden] [--enabled] [--disabled]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID |
| `--iap-id` |  |  | IAP ID to promote (mutually exclusive with --subscription-id) |
| `--subscription-id` |  |  | Subscription ID to promote (mutually exclusive with --iap-id) |
| `--visible` |  |  | Make visible for all users |
| `--hidden` |  |  | Hide from all users |
| `--enabled` |  |  | Enable the slot |
| `--disabled` |  |  | Disable the slot |

### asc promoted-purchases update

Update a promoted purchase slot's visibility or enabled state

```
asc promoted-purchases update --promoted-id <promoted-id> [--visible] [--hidden] [--enabled] [--disabled]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--promoted-id` | yes |  | Promoted purchase ID |
| `--visible` |  |  | Make visible for all users |
| `--hidden` |  |  | Hide from all users |
| `--enabled` |  |  | Enable the slot |
| `--disabled` |  |  | Disable the slot |

### asc promoted-purchases delete

Delete a promoted purchase slot

```
asc promoted-purchases delete --promoted-id <promoted-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--promoted-id` | yes |  | Promoted purchase ID |

## asc experiments

Manage product page optimization tests (App Store version experiments)

### asc experiments list

List product page optimization tests for an app

```
asc experiments list --app-id <app-id> [--state <state>] [--limit <limit>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID |
| `--state` |  |  | Filter by state (e.g. PREPARE_FOR_SUBMISSION, APPROVED, COMPLETED) |
| `--limit` |  |  | Max results |

### asc experiments get

Get a product page optimization test

```
asc experiments get --experiment-id <experiment-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--experiment-id` | yes |  | Experiment ID |

### asc experiments create

Create a product page optimization test

```
asc experiments create --app-id <app-id> --name <name> --traffic-proportion <traffic-proportion> [--platform <platform>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID |
| `--name` | yes |  | Reference name shown in App Analytics |
| `--platform` |  | `ios` | Platform: ios, macos, tvos, visionos |
| `--traffic-proportion` | yes |  | Percentage of users (1-100) shown a treatment instead of the original page |

### asc experiments update

Rename a test or change its traffic proportion

```
asc experiments update --experiment-id <experiment-id> [--name <name>] [--traffic-proportion <traffic-proportion>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--experiment-id` | yes |  | Experiment ID |
| `--name` |  |  | New reference name |
| `--traffic-proportion` |  |  | New traffic proportion (1-100) |

### asc experiments start

Start an approved test so it begins serving traffic

```
asc experiments start --experiment-id <experiment-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--experiment-id` | yes |  | Experiment ID |

### asc experiments stop

Stop a running test

```
asc experiments stop --experiment-id <experiment-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--experiment-id` | yes |  | Experiment ID |

### asc experiments delete

Delete a product page optimization test

```
asc experiments delete --experiment-id <experiment-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--experiment-id` | yes |  | Experiment ID |

## asc experiment-treatments

Manage treatments (product page variants) of a product page optimization test

### asc experiment-treatments list

List treatments of a test

```
asc experiment-treatments list --experiment-id <experiment-id> [--limit <limit>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--experiment-id` | yes |  | Experiment ID |
| `--limit` |  |  | Max results |

### asc experiment-treatments create

Add a treatment (up to three per test)

```
asc experiment-treatments create --experiment-id <experiment-id> --name <name> [--app-icon-name <app-icon-name>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--experiment-id` | yes |  | Experiment ID |
| `--name` | yes |  | Treatment name |
| `--app-icon-name` |  |  | Alternate app icon asset name to test |

### asc experiment-treatments update

Rename a treatment or change its app icon

```
asc experiment-treatments update --treatment-id <treatment-id> [--name <name>] [--app-icon-name <app-icon-name>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--treatment-id` | yes |  | Treatment ID |
| `--name` |  |  | New treatment name |
| `--app-icon-name` |  |  | Alternate app icon asset name |

### asc experiment-treatments delete

Delete a treatment

```
asc experiment-treatments delete --treatment-id <treatment-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--treatment-id` | yes |  | Treatment ID |

## asc experiment-treatment-localizations

Manage the locales included in a treatment

### asc experiment-treatment-localizations list

List the locales included in a treatment

```
asc experiment-treatment-localizations list --treatment-id <treatment-id> [--limit <limit>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--treatment-id` | yes |  | Treatment ID |
| `--limit` |  |  | Max results |

### asc experiment-treatment-localizations create

Include a locale in a treatment

```
asc experiment-treatment-localizations create --treatment-id <treatment-id> --locale <locale>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--treatment-id` | yes |  | Treatment ID |
| `--locale` | yes |  | Locale code (e.g. en-US) |

### asc experiment-treatment-localizations delete

Remove a locale from a treatment

```
asc experiment-treatment-localizations delete --localization-id <localization-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--localization-id` | yes |  | Treatment localization ID |

## asc iap-review-screenshot

Manage the App Store review screenshot for an in-app purchase

### asc iap-review-screenshot get

Fetch the review screenshot for an in-app purchase

```
asc iap-review-screenshot get --iap-id <iap-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--iap-id` | yes |  | IAP ID |

### asc iap-review-screenshot upload

Upload (or replace) the App Store review screenshot for an in-app purchase

```
asc iap-review-screenshot upload --iap-id <iap-id> --file <file>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--iap-id` | yes |  | IAP ID |
| `--file` | yes |  | Path to image file |

### asc iap-review-screenshot delete

Delete the review screenshot

```
asc iap-review-screenshot delete --screenshot-id <screenshot-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--screenshot-id` | yes |  | Review screenshot ID |

## asc iap-images

Manage 1024x1024 promotional images for an in-app purchase

### asc iap-images list

List promotional images for an in-app purchase

```
asc iap-images list --iap-id <iap-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--iap-id` | yes |  | IAP ID |

### asc iap-images upload

Upload a 1024x1024 promotional image for an in-app purchase

```
asc iap-images upload --iap-id <iap-id> --file <file>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--iap-id` | yes |  | IAP ID |
| `--file` | yes |  | Path to image file |

### asc iap-images delete

Delete a promotional image

```
asc iap-images delete --image-id <image-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--image-id` | yes |  | Image ID |

## asc subscription-review-screenshot

Manage the App Store review screenshot for a subscription

### asc subscription-review-screenshot get

Fetch the review screenshot for a subscription

```
asc subscription-review-screenshot get --subscription-id <subscription-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID |

### asc subscription-review-screenshot upload

Upload (or replace) the App Store review screenshot for a subscription

```
asc subscription-review-screenshot upload --subscription-id <subscription-id> --file <file>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID |
| `--file` | yes |  | Path to image file |

### asc subscription-review-screenshot delete

Delete the subscription review screenshot

```
asc subscription-review-screenshot delete --screenshot-id <screenshot-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--screenshot-id` | yes |  | Review screenshot ID |

## asc subscription-images

Manage promotional images for a subscription

### asc subscription-images list

List promotional images for a subscription

```
asc subscription-images list --subscription-id <subscription-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID |

### asc subscription-images upload

Upload a promotional image for a subscription

```
asc subscription-images upload --subscription-id <subscription-id> --file <file>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID |
| `--file` | yes |  | Path to image file |

### asc subscription-images delete

Delete a subscription promotional image

```
asc subscription-images delete --image-id <image-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--image-id` | yes |  | Image ID |

## asc subscription-offer-codes

Manage subscription offer codes

### asc subscription-offer-codes list

List offer codes for a subscription

```
asc subscription-offer-codes list --subscription-id <subscription-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID |

### asc subscription-offer-codes create

Create an offer code for a subscription

```
asc subscription-offer-codes create --subscription-id <subscription-id> --name <name> --duration <duration> --mode <mode> --periods <periods> --eligibility <eligibility> … --offer-eligibility <offer-eligibility> [--auto-renew <auto-renew>] [--price <price> …] [--free-territory <free-territory> …]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID |
| `--name` | yes |  | Offer code name |
| `--duration` | yes |  | Offer duration: THREE_DAYS, ONE_WEEK, TWO_WEEKS, ONE_MONTH, TWO_MONTHS, THREE_MONTHS, SIX_MONTHS, ONE_YEAR |
| `--mode` | yes |  | Offer mode: PAY_AS_YOU_GO, PAY_UP_FRONT, FREE_TRIAL |
| `--periods` | yes |  | Number of periods |
| `--eligibility` | yes |  | Customer eligibility: NEW, LAPSED, WIN_BACK, PAID_SUBSCRIBER (repeatable) |
| `--offer-eligibility` | yes |  | Offer eligibility: STACKABLE, INTRODUCTORY, SUBSCRIPTION_OFFER |
| `--auto-renew` |  | `true` | Whether the offer auto-renews after the offer period (default: true). Set --auto-renew false for non-renewing (one-time) offers. |
| `--price` |  |  | Paid price for a territory in `<territory>=<price-point-id>` form (repeatable) |
| `--free-territory` |  |  | Territory that should redeem the offer for free (repeatable) |

### asc subscription-offer-codes update

Update an offer code (activate/deactivate)

```
asc subscription-offer-codes update --offer-code-id <offer-code-id> --active <active>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--offer-code-id` | yes |  | Offer code ID |
| `--active` | yes |  | Active status (true/false) |

### asc subscription-offer-codes prices list

List per-territory prices for a subscription offer code

```
asc subscription-offer-codes prices list --offer-code-id <offer-code-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--offer-code-id` | yes |  | Offer code ID |

## asc subscription-offer-code-custom-codes

Manage subscription offer code custom codes

### asc subscription-offer-code-custom-codes list

List custom codes for a subscription offer code

```
asc subscription-offer-code-custom-codes list --offer-code-id <offer-code-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--offer-code-id` | yes |  | Offer code ID |

### asc subscription-offer-code-custom-codes create

Create a custom code for a subscription offer code

```
asc subscription-offer-code-custom-codes create --offer-code-id <offer-code-id> --custom-code <custom-code> --number-of-codes <number-of-codes> [--expiration-date <expiration-date>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--offer-code-id` | yes |  | Offer code ID |
| `--custom-code` | yes |  | Custom code string |
| `--number-of-codes` | yes |  | Number of codes to generate |
| `--expiration-date` |  |  | Expiration date in YYYY-MM-DD format — optional |

### asc subscription-offer-code-custom-codes update

Update a custom code (activate/deactivate)

```
asc subscription-offer-code-custom-codes update --custom-code-id <custom-code-id> --active <active>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--custom-code-id` | yes |  | Custom code ID |
| `--active` | yes |  | Active status (true/false) |

## asc subscription-offer-code-one-time-codes

Manage subscription offer code one-time use codes

### asc subscription-offer-code-one-time-codes list

List one-time use codes for a subscription offer code

```
asc subscription-offer-code-one-time-codes list --offer-code-id <offer-code-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--offer-code-id` | yes |  | Offer code ID |

### asc subscription-offer-code-one-time-codes create

Create one-time use codes for a subscription offer code

```
asc subscription-offer-code-one-time-codes create --offer-code-id <offer-code-id> --number-of-codes <number-of-codes> --expiration-date <expiration-date> [--environment <environment>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--offer-code-id` | yes |  | Offer code ID |
| `--number-of-codes` | yes |  | Number of codes to generate |
| `--expiration-date` | yes |  | Expiration date in YYYY-MM-DD format |
| `--environment` |  | `PRODUCTION` | Redemption environment (production or sandbox) |

### asc subscription-offer-code-one-time-codes update

Update a one-time use code (activate/deactivate)

```
asc subscription-offer-code-one-time-codes update --one-time-code-id <one-time-code-id> --active <active>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--one-time-code-id` | yes |  | One-time code ID |
| `--active` | yes |  | Active status (true/false) |

### asc subscription-offer-code-one-time-codes values

Fetch the redemption values (CSV) for a subscription one-time code batch

```
asc subscription-offer-code-one-time-codes values --one-time-code-id <one-time-code-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--one-time-code-id` | yes |  | One-time code batch ID |

## asc iap-offer-codes

Manage in-app purchase offer codes

### asc iap-offer-codes list

List offer codes for an in-app purchase

```
asc iap-offer-codes list --iap-id <iap-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--iap-id` | yes |  | In-app purchase ID |

### asc iap-offer-codes create

Create an offer code for an in-app purchase

```
asc iap-offer-codes create --iap-id <iap-id> --name <name> --eligibility <eligibility> … [--price <price> …] [--free-territory <free-territory> …]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--iap-id` | yes |  | In-app purchase ID |
| `--name` | yes |  | Offer code name |
| `--eligibility` | yes |  | Customer eligibility: NON_SPENDER, ACTIVE_SPENDER, CHURNED_SPENDER (repeatable) |
| `--price` |  |  | Paid price for a territory in `<territory>=<price-point-id>` form (repeatable) |
| `--free-territory` |  |  | Territory that should redeem the offer for free (repeatable) |

### asc iap-offer-codes update

Update an IAP offer code (activate/deactivate)

```
asc iap-offer-codes update --offer-code-id <offer-code-id> --active <active>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--offer-code-id` | yes |  | Offer code ID |
| `--active` | yes |  | Active status (true/false) |

### asc iap-offer-codes prices list

List per-territory prices for an IAP offer code

```
asc iap-offer-codes prices list --offer-code-id <offer-code-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--offer-code-id` | yes |  | Offer code ID |

## asc iap-offer-code-custom-codes

Manage in-app purchase offer code custom codes

### asc iap-offer-code-custom-codes list

List custom codes for an IAP offer code

```
asc iap-offer-code-custom-codes list --offer-code-id <offer-code-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--offer-code-id` | yes |  | Offer code ID |

### asc iap-offer-code-custom-codes create

Create a custom code for an IAP offer code

```
asc iap-offer-code-custom-codes create --offer-code-id <offer-code-id> --custom-code <custom-code> --number-of-codes <number-of-codes> [--expiration-date <expiration-date>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--offer-code-id` | yes |  | Offer code ID |
| `--custom-code` | yes |  | Custom code string |
| `--number-of-codes` | yes |  | Number of codes to generate |
| `--expiration-date` |  |  | Expiration date in YYYY-MM-DD format — optional |

### asc iap-offer-code-custom-codes update

Update an IAP custom code (activate/deactivate)

```
asc iap-offer-code-custom-codes update --custom-code-id <custom-code-id> --active <active>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--custom-code-id` | yes |  | Custom code ID |
| `--active` | yes |  | Active status (true/false) |

## asc iap-offer-code-one-time-codes

Manage in-app purchase offer code one-time use codes

### asc iap-offer-code-one-time-codes list

List one-time use codes for an IAP offer code

```
asc iap-offer-code-one-time-codes list --offer-code-id <offer-code-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--offer-code-id` | yes |  | Offer code ID |

### asc iap-offer-code-one-time-codes create

Create one-time use codes for an IAP offer code

```
asc iap-offer-code-one-time-codes create --offer-code-id <offer-code-id> --number-of-codes <number-of-codes> --expiration-date <expiration-date> [--environment <environment>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--offer-code-id` | yes |  | Offer code ID |
| `--number-of-codes` | yes |  | Number of codes to generate |
| `--expiration-date` | yes |  | Expiration date in YYYY-MM-DD format |
| `--environment` |  | `PRODUCTION` | Redemption environment (production or sandbox) |

### asc iap-offer-code-one-time-codes update

Update an IAP one-time use code (activate/deactivate)

```
asc iap-offer-code-one-time-codes update --one-time-code-id <one-time-code-id> --active <active>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--one-time-code-id` | yes |  | One-time code ID |
| `--active` | yes |  | Active status (true/false) |

### asc iap-offer-code-one-time-codes values

Fetch the redemption values (CSV) for an IAP one-time code batch

```
asc iap-offer-code-one-time-codes values --one-time-code-id <one-time-code-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--one-time-code-id` | yes |  | One-time code batch ID |

## asc app-shots

AI-powered App Store screenshot planning and generation

### asc app-shots templates list

List available screenshot templates

```
asc app-shots templates list [--size <size>] [--preview]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--size` |  |  | Filter by size: portrait, landscape, portrait43, square One of: portrait, portrait43, landscape, square. |
| `--preview` |  |  | Include self-contained HTML preview for each template |

### asc app-shots templates get

Get details of a specific template

```
asc app-shots templates get --id <id> [--preview]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--id` | yes |  | Template ID |
| `--preview` |  |  | Output self-contained HTML preview page |

### asc app-shots templates apply

Apply a template to a screenshot — returns the composed design with preview

```
asc app-shots templates apply --id <id> --screenshot <screenshot> --headline <headline> [--subtitle <subtitle>] [--tagline <tagline>] [--app-name <app-name>] [--preview <preview>] [--image-output <image-output>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--id` | yes |  | Template ID |
| `--screenshot` | yes |  | Path to screenshot file |
| `--headline` | yes |  | Headline text |
| `--subtitle` |  |  | Subtitle text |
| `--tagline` |  |  | Tagline text (overrides template default) |
| `--app-name` |  | `My App` | App name |
| `--preview` |  |  | Preview format: html (default) or image (renders to PNG) One of: html, image. |
| `--image-output` |  |  | Output PNG path (for --preview image) |

### asc app-shots gallery-templates list

List available gallery templates

```
asc app-shots gallery-templates list
```

### asc app-shots gallery-templates get

Get details of a specific gallery template

```
asc app-shots gallery-templates get --id <id> [--preview]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--id` | yes |  | Gallery template ID |
| `--preview` |  |  | Output self-contained HTML gallery preview page |

### asc app-shots themes list

List available visual themes

```
asc app-shots themes list
```

### asc app-shots themes get

Get details of a specific theme

```
asc app-shots themes get --id <id> [--context]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--id` | yes |  | Theme ID |
| `--context` |  |  | Output the buildContext() prompt string instead of JSON |

### asc app-shots themes design

Generate a ThemeDesign (palette + decorations) from AI — one call, reusable

```
asc app-shots themes design --id <id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--id` | yes |  | Theme ID |

### asc app-shots themes apply-design

Apply a cached ThemeDesign to a template — deterministic, no AI

```
asc app-shots themes apply-design --design <design> --template <template> --screenshot <screenshot> [--headline <headline>] [--subtitle <subtitle>] [--tagline <tagline>] [--preview <preview>] [--image-output <image-output>] [--canvas-width <canvas-width>] [--canvas-height <canvas-height>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--design` | yes |  | Path to ThemeDesign JSON file |
| `--template` | yes |  | Template ID |
| `--screenshot` | yes |  | Path to screenshot file |
| `--headline` |  | `Your Headline` | Headline text |
| `--subtitle` |  |  | Subtitle text |
| `--tagline` |  |  | Tagline text |
| `--preview` |  |  | Preview format: html (default) or image One of: html, image. |
| `--image-output` |  |  | Output PNG path (for --preview image) |
| `--canvas-width` |  | `1320` | Canvas width in pixels |
| `--canvas-height` |  | `2868` | Canvas height in pixels |

### asc app-shots themes apply

Apply a theme via full AI restyle

```
asc app-shots themes apply --theme <theme> --template <template> --screenshot <screenshot> [--headline <headline>] [--subtitle <subtitle>] [--tagline <tagline>] [--canvas-width <canvas-width>] [--canvas-height <canvas-height>] [--preview <preview>] [--image-output <image-output>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--theme` | yes |  | Theme ID |
| `--template` | yes |  | Template ID |
| `--screenshot` | yes |  | Path to screenshot file |
| `--headline` |  | `Your Headline` | Headline text |
| `--subtitle` |  |  | Subtitle text |
| `--tagline` |  |  | Tagline text (overrides template default) |
| `--canvas-width` |  | `1320` | Canvas width in pixels (default: 1320) |
| `--canvas-height` |  | `2868` | Canvas height in pixels (default: 2868) |
| `--preview` |  |  | Preview format: html (default) or image (renders to PNG) One of: html, image. |
| `--image-output` |  |  | Output PNG path (for --preview image) |

### asc app-shots generate

Enhance App Store screenshots using Gemini AI

```
asc app-shots generate --file <file> [--gemini-api-key <gemini-api-key>] [--model <model>] [--output-dir <output-dir>] [--style-reference <style-reference>] [--device-type <device-type>] [--prompt <prompt>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--file` | yes |  | Screenshot file to enhance |
| `--gemini-api-key` |  |  | Gemini API key (falls back to GEMINI_API_KEY env var) |
| `--model` |  | `gemini-3.1-flash-image-preview` | Gemini model |
| `--output-dir` |  | `.asc/app-shots/output` | Output directory |
| `--style-reference` |  |  | Style reference image — Gemini replicates its visual style |
| `--device-type` |  |  | Named device type — resizes output to exact App Store dimensions. E.g.: APP_IPHONE_67 (1290×2796) One of: APP_IPHONE_69, APP_IPHONE_67, APP_IPHONE_65, APP_IPHONE_61, APP_IPHONE_58, APP_IPHONE_55, APP_IPHONE_47, APP_IPHONE_40, APP_IPHONE_35, APP_IPAD_PRO_129, APP_IPAD_PRO_3GEN_11, APP_IPAD_105, APP_IPAD_97, APP_APPLE_TV, APP_DESKTOP, APP_APPLE_VISION_PRO. |
| `--prompt` |  |  | Custom enhancement prompt |

### asc app-shots export

Render an HTML file to PNG via WebKit

```
asc app-shots export --html <html> [--width <width>] [--height <height>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--html` | yes |  | Path to HTML file to render |
| `--width` |  | `1320` | Width in pixels (default: 1320) |
| `--height` |  | `2868` | Height in pixels (default: 2868) |

### asc app-shots config

Configure app-shots settings (Gemini API key)

```
asc app-shots config [--gemini-api-key <gemini-api-key>] [--remove]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--gemini-api-key` |  |  | Gemini API key to save for app-shots generation |
| `--remove` |  |  | Remove saved app-shots configuration |

## asc age-rating

Manage App Store age rating declarations

### asc age-rating get

Get the age rating declaration for an app info

```
asc age-rating get --app-info-id <app-info-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-info-id` | yes |  | App Info ID |

### asc age-rating update

Update the age rating declaration

```
asc age-rating update --declaration-id <declaration-id> [--advertising <advertising>] [--gambling <gambling>] [--health-or-wellness-topics <health-or-wellness-topics>] [--loot-box <loot-box>] [--messaging-and-chat <messaging-and-chat>] [--parental-controls <parental-controls>] [--age-assurance <age-assurance>] [--unrestricted-web-access <unrestricted-web-access>] [--user-generated-content <user-generated-content>] [--alcohol-tobacco-drugs <alcohol-tobacco-drugs>] [--contests <contests>] [--gambling-simulated <gambling-simulated>] [--guns-weapons <guns-weapons>] [--medical-treatment <medical-treatment>] [--profanity <profanity>] [--sexual-content-graphic <sexual-content-graphic>] [--sexual-content <sexual-content>] [--horror-fear <horror-fear>] [--mature-suggestive <mature-suggestive>] [--violence-cartoon <violence-cartoon>] [--violence-realistic-prolonged <violence-realistic-prolonged>] [--violence-realistic <violence-realistic>] [--kids-age-band <kids-age-band>] [--age-rating-override <age-rating-override>] [--korea-age-rating-override <korea-age-rating-override>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--declaration-id` | yes |  | Age Rating Declaration ID |
| `--advertising` |  |  | Contains advertising (true/false) |
| `--gambling` |  |  | Contains gambling (true/false) |
| `--health-or-wellness-topics` |  |  | Contains health or wellness topics (true/false) |
| `--loot-box` |  |  | Contains loot box mechanics (true/false) |
| `--messaging-and-chat` |  |  | Contains messaging or chat (true/false) |
| `--parental-controls` |  |  | Contains parental controls (true/false) |
| `--age-assurance` |  |  | Contains age assurance features (true/false) |
| `--unrestricted-web-access` |  |  | Contains unrestricted web access (true/false) |
| `--user-generated-content` |  |  | Contains user-generated content (true/false) |
| `--alcohol-tobacco-drugs` |  |  | Alcohol, tobacco or drug use intensity One of: NONE, INFREQUENT_OR_MILD, FREQUENT_OR_INTENSE, INFREQUENT, FREQUENT. |
| `--contests` |  |  | Contests intensity One of: NONE, INFREQUENT_OR_MILD, FREQUENT_OR_INTENSE, INFREQUENT, FREQUENT. |
| `--gambling-simulated` |  |  | Simulated gambling intensity One of: NONE, INFREQUENT_OR_MILD, FREQUENT_OR_INTENSE, INFREQUENT, FREQUENT. |
| `--guns-weapons` |  |  | Guns or other weapons intensity One of: NONE, INFREQUENT_OR_MILD, FREQUENT_OR_INTENSE, INFREQUENT, FREQUENT. |
| `--medical-treatment` |  |  | Medical or treatment information intensity One of: NONE, INFREQUENT_OR_MILD, FREQUENT_OR_INTENSE, INFREQUENT, FREQUENT. |
| `--profanity` |  |  | Profanity or crude humor intensity One of: NONE, INFREQUENT_OR_MILD, FREQUENT_OR_INTENSE, INFREQUENT, FREQUENT. |
| `--sexual-content-graphic` |  |  | Sexual content (graphic and nudity) intensity One of: NONE, INFREQUENT_OR_MILD, FREQUENT_OR_INTENSE, INFREQUENT, FREQUENT. |
| `--sexual-content` |  |  | Sexual content or nudity intensity One of: NONE, INFREQUENT_OR_MILD, FREQUENT_OR_INTENSE, INFREQUENT, FREQUENT. |
| `--horror-fear` |  |  | Horror or fear themes intensity One of: NONE, INFREQUENT_OR_MILD, FREQUENT_OR_INTENSE, INFREQUENT, FREQUENT. |
| `--mature-suggestive` |  |  | Mature or suggestive themes intensity One of: NONE, INFREQUENT_OR_MILD, FREQUENT_OR_INTENSE, INFREQUENT, FREQUENT. |
| `--violence-cartoon` |  |  | Cartoon or fantasy violence intensity One of: NONE, INFREQUENT_OR_MILD, FREQUENT_OR_INTENSE, INFREQUENT, FREQUENT. |
| `--violence-realistic-prolonged` |  |  | Realistic prolonged, graphic or sadistic violence intensity One of: NONE, INFREQUENT_OR_MILD, FREQUENT_OR_INTENSE, INFREQUENT, FREQUENT. |
| `--violence-realistic` |  |  | Realistic violence intensity One of: NONE, INFREQUENT_OR_MILD, FREQUENT_OR_INTENSE, INFREQUENT, FREQUENT. |
| `--kids-age-band` |  |  | Kids age band (FIVE_AND_UNDER/SIX_TO_EIGHT/NINE_TO_ELEVEN) One of: FIVE_AND_UNDER, SIX_TO_EIGHT, NINE_TO_ELEVEN. |
| `--age-rating-override` |  |  | Age rating override (NONE/NINE_PLUS/THIRTEEN_PLUS/SIXTEEN_PLUS/EIGHTEEN_PLUS/UNRATED) One of: NONE, NINE_PLUS, THIRTEEN_PLUS, SIXTEEN_PLUS, EIGHTEEN_PLUS, UNRATED. |
| `--korea-age-rating-override` |  |  | Korea age rating override (NONE/FIFTEEN_PLUS/NINETEEN_PLUS) One of: NONE, FIFTEEN_PLUS, NINETEEN_PLUS. |

## asc app-categories

List App Store categories

### asc app-categories list

List available App Store categories and subcategories

```
asc app-categories list [--platform <platform>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--platform` |  |  | Filter by platform: IOS, MAC_OS, TV_OS, VISION_OS |

### asc app-categories get

Get a single App Store category by ID

```
asc app-categories get --category-id <category-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--category-id` | yes |  | Category ID (e.g. 6014 for Games, 6014.6014-action for Games/Action) |

## asc version-review-detail

Manage App Store version review information

### asc version-review-detail get

Get review contact info for an App Store version

```
asc version-review-detail get --version-id <version-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--version-id` | yes |  | Version ID |

### asc version-review-detail update

Set or update review contact info for an App Store version (upserts)

```
asc version-review-detail update --version-id <version-id> [--contact-first-name <contact-first-name>] [--contact-last-name <contact-last-name>] [--contact-phone <contact-phone>] [--contact-email <contact-email>] [--demo-account-required <demo-account-required>] [--demo-account-name <demo-account-name>] [--demo-account-password <demo-account-password>] [--notes <notes>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--version-id` | yes |  | Version ID |
| `--contact-first-name` |  |  | Reviewer's first name |
| `--contact-last-name` |  |  | Reviewer's last name |
| `--contact-phone` |  |  | Reviewer's phone number |
| `--contact-email` |  |  | Reviewer's email address |
| `--demo-account-required` |  |  | Demo account required (true/false) |
| `--demo-account-name` |  |  | Demo account username |
| `--demo-account-password` |  |  | Demo account password |
| `--notes` |  |  | Notes for the reviewer |

## asc plugins

Manage installed plugins and browse the plugin marketplace

### asc plugins list

List installed plugins

```
asc plugins list
```

### asc plugins install

Install a plugin from the marketplace

```
asc plugins install --name <name>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--name` | yes |  | Plugin name to install |

### asc plugins uninstall

Uninstall a plugin

```
asc plugins uninstall --name <name>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--name` | yes |  | Plugin name to uninstall |

### asc plugins updates

List installed plugins that have a newer version available

```
asc plugins updates
```

### asc plugins update

Update an installed plugin to the latest marketplace version

```
asc plugins update --name <name>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--name` | yes |  | Plugin name (matches `Plugin.name` from `asc plugins list`) |

### asc plugins market list

List all available plugins in the marketplace

```
asc plugins market list
```

### asc plugins market search

Search plugins in the marketplace

```
asc plugins market search --query <query>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--query` | yes |  | Search query |

## asc skills

Manage Claude Code agent skills from the asc-cli repository

### asc skills list

List available skills from the asc-cli repository

```
asc skills list
```

### asc skills install

Install skills from the asc-cli repository

```
asc skills install [--name <name>] [--all]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--name` |  |  | Name of a specific skill to install |
| `--all` |  |  | Install all available skills |

### asc skills uninstall

Remove an installed skill

```
asc skills uninstall --name <name>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--name` | yes |  | Skill name to uninstall |

### asc skills installed

Show skills installed in ~/.claude/skills/

```
asc skills installed
```

### asc skills check

Check for available skill updates

```
asc skills check
```

### asc skills update

Update installed skills to the latest version

```
asc skills update
```

## asc app-wall

Submit your developer profile to the asc app wall

### asc app-wall submit

Submit your app to the asc app wall by opening a pull request on GitHub

```
asc app-wall submit [--developer <developer>] [--developer-id <developer-id>] [--github <github>] [--x <x>] [--app-id <app-id> …] [--app <app> …] [--github-token <github-token>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--developer` |  |  | Your developer display name |
| `--developer-id` |  |  | Your Apple developer/seller ID — auto-fetches all your App Store apps |
| `--github` |  |  | Your GitHub username |
| `--x` |  |  | Your X (Twitter) handle |
| `--app-id` |  |  | App Store Connect app ID (repeat for multiple apps) |
| `--app` |  |  | Specific App Store URL (repeat for multiple apps) |
| `--github-token` |  |  | GitHub personal access token (or set GITHUB_TOKEN) |

## asc users

Manage App Store Connect team members

### asc users list

List team members

```
asc users list [--role <role>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--role` |  |  | Filter by role (e.g. ADMIN, DEVELOPER, APP_MANAGER) |

### asc users update

Update a team member's roles

```
asc users update --user-id <user-id> --role <role> …
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--user-id` | yes |  | User resource ID |
| `--role` | yes |  | Role to assign (repeatable, e.g. --role ADMIN --role DEVELOPER) |

### asc users remove

Remove a team member from App Store Connect

```
asc users remove --user-id <user-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--user-id` | yes |  | User resource ID |

## asc user-invitations

Manage pending App Store Connect user invitations

### asc user-invitations list

List pending user invitations

```
asc user-invitations list [--role <role>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--role` |  |  | Filter by role (e.g. ADMIN, DEVELOPER) |

### asc user-invitations invite

Send an invitation to join App Store Connect

```
asc user-invitations invite --email <email> --first-name <first-name> --last-name <last-name> --role <role> … [--all-apps-visible]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--email` | yes |  | Email address of the invitee |
| `--first-name` | yes |  | First name of the invitee |
| `--last-name` | yes |  | Last name of the invitee |
| `--role` | yes |  | Role to assign (repeatable, e.g. --role ADMIN --role DEVELOPER) |
| `--all-apps-visible` |  |  | Grant access to all apps (default: false) |

### asc user-invitations cancel

Cancel a pending user invitation

```
asc user-invitations cancel --invitation-id <invitation-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--invitation-id` | yes |  | User invitation resource ID |

## asc xcode-cloud

Read Xcode Cloud data, manage workflows, and start builds

### asc xcode-cloud products list

List Xcode Cloud products

```
asc xcode-cloud products list [--app-id <app-id>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` |  |  | Filter by app ID |

### asc xcode-cloud workflows list

List workflows for an Xcode Cloud product

```
asc xcode-cloud workflows list --product-id <product-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--product-id` | yes |  | Xcode Cloud product ID |

### asc xcode-cloud builds list

List build runs for a workflow

```
asc xcode-cloud builds list --workflow-id <workflow-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--workflow-id` | yes |  | Workflow ID |

### asc xcode-cloud builds get

Get a specific build run

```
asc xcode-cloud builds get --build-run-id <build-run-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--build-run-id` | yes |  | Build run ID |

### asc xcode-cloud builds start

Start a new build run for a workflow

```
asc xcode-cloud builds start --workflow-id <workflow-id> [--clean]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--workflow-id` | yes |  | Workflow ID |
| `--clean` |  |  | Perform a clean build |

## asc game-center

Manage Game Center achievements and leaderboards

### asc game-center detail get

Get Game Center configuration for an app

```
asc game-center detail get --app-id <app-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID |

### asc game-center achievements list

List achievements for a Game Center detail

```
asc game-center achievements list --detail-id <detail-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--detail-id` | yes |  | Game Center detail ID |

### asc game-center achievements create

Create a new Game Center achievement

```
asc game-center achievements create --detail-id <detail-id> --reference-name <reference-name> --vendor-identifier <vendor-identifier> --points <points> [--show-before-earned] [--repeatable]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--detail-id` | yes |  | Game Center detail ID |
| `--reference-name` | yes |  | Internal reference name |
| `--vendor-identifier` | yes |  | Unique vendor identifier |
| `--points` | yes |  | Point value for the achievement |
| `--show-before-earned` |  |  | Show achievement in UI before it is earned |
| `--repeatable` |  |  | Allow players to earn this achievement multiple times |

### asc game-center achievements delete

Delete a Game Center achievement

```
asc game-center achievements delete --achievement-id <achievement-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--achievement-id` | yes |  | Achievement ID |

### asc game-center leaderboards list

List leaderboards for a Game Center detail

```
asc game-center leaderboards list --detail-id <detail-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--detail-id` | yes |  | Game Center detail ID |

### asc game-center leaderboards create

Create a new Game Center leaderboard

```
asc game-center leaderboards create --detail-id <detail-id> --reference-name <reference-name> --vendor-identifier <vendor-identifier> --score-sort-type <score-sort-type> [--submission-type <submission-type>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--detail-id` | yes |  | Game Center detail ID |
| `--reference-name` | yes |  | Internal reference name |
| `--vendor-identifier` | yes |  | Unique vendor identifier |
| `--score-sort-type` | yes |  | Score sort order: ASC or DESC |
| `--submission-type` |  | `BEST_SCORE` | Submission type: BEST_SCORE or MOST_RECENT_SCORE (default: BEST_SCORE) |

### asc game-center leaderboards delete

Delete a Game Center leaderboard

```
asc game-center leaderboards delete --leaderboard-id <leaderboard-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--leaderboard-id` | yes |  | Leaderboard ID |

## asc app-clips

Manage App Clips

### asc app-clips list

List App Clips for an app

```
asc app-clips list --app-id <app-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID |

## asc app-clip-experiences

Manage App Clip default experiences

### asc app-clip-experiences list

List default experiences for an App Clip

```
asc app-clip-experiences list --app-clip-id <app-clip-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-clip-id` | yes |  | App Clip ID |

### asc app-clip-experiences create

Create a default experience for an App Clip

```
asc app-clip-experiences create --app-clip-id <app-clip-id> [--action <action>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-clip-id` | yes |  | App Clip ID |
| `--action` |  |  | Action: OPEN, VIEW, or PLAY |

### asc app-clip-experiences delete

Delete a default experience

```
asc app-clip-experiences delete --experience-id <experience-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--experience-id` | yes |  | Experience ID |

## asc app-clip-experience-localizations

Manage App Clip default experience localizations

### asc app-clip-experience-localizations list

List localizations for a default experience

```
asc app-clip-experience-localizations list --experience-id <experience-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--experience-id` | yes |  | Experience ID |

### asc app-clip-experience-localizations create

Create a localization for a default experience

```
asc app-clip-experience-localizations create --experience-id <experience-id> --locale <locale> [--subtitle <subtitle>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--experience-id` | yes |  | Experience ID |
| `--locale` | yes |  | Locale code (e.g. en-US, fr-FR) |
| `--subtitle` |  |  | Subtitle shown in the App Clip card |

### asc app-clip-experience-localizations delete

Delete a localization

```
asc app-clip-experience-localizations delete --localization-id <localization-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--localization-id` | yes |  | Localization ID |

## asc sales-reports

Download sales and trends reports

### asc sales-reports download

Download a sales report

```
asc sales-reports download --report-type <report-type> --sub-type <sub-type> --frequency <frequency> [--vendor-number <vendor-number>] [--report-date <report-date>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--vendor-number` |  |  | Vendor number (auto-resolved from active account if saved) |
| `--report-type` | yes |  | Report type: SALES, PRE_ORDER, SUBSCRIPTION, etc. |
| `--sub-type` | yes |  | Report sub-type: SUMMARY, DETAILED, etc. |
| `--frequency` | yes |  | Frequency: DAILY, WEEKLY, MONTHLY, YEARLY |
| `--report-date` |  |  | Report date (e.g. 2024-01-15) |

### asc sales-reports summary

Aggregate daily sales reports into a single rollup (downloads, updates, in-app purchases, payers, and per-currency customer spend and developer proceeds)

```
asc sales-reports summary --from <from> --to <to> [--vendor-number <vendor-number>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--vendor-number` |  |  | Vendor number (auto-resolved from active account if saved) |
| `--from` | yes |  | Start date inclusive (YYYY-MM-DD) |
| `--to` | yes |  | End date inclusive (YYYY-MM-DD) |

## asc finance-reports

Download financial reports

### asc finance-reports download

Download a financial report

```
asc finance-reports download --report-type <report-type> --region-code <region-code> --report-date <report-date> [--vendor-number <vendor-number>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--vendor-number` |  |  | Vendor number (auto-resolved from active account if saved) |
| `--report-type` | yes |  | Report type: FINANCIAL, FINANCE_DETAIL |
| `--region-code` | yes |  | Region code (e.g. US, EU, JP) |
| `--report-date` | yes |  | Report date (e.g. 2024-01) |

## asc analytics-reports

Manage analytics reports

### asc analytics-reports request

Create an analytics report request for an app

```
asc analytics-reports request --app-id <app-id> --access-type <access-type>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID |
| `--access-type` | yes |  | Access type: ONE_TIME_SNAPSHOT, ONGOING |

### asc analytics-reports list

List analytics report requests for an app

```
asc analytics-reports list --app-id <app-id> [--access-type <access-type>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID |
| `--access-type` |  |  | Filter by access type: ONE_TIME_SNAPSHOT, ONGOING |

### asc analytics-reports delete

Delete an analytics report request

```
asc analytics-reports delete --request-id <request-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--request-id` | yes |  | Request ID |

### asc analytics-reports reports

List available reports for a request

```
asc analytics-reports reports --request-id <request-id> [--category <category>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--request-id` | yes |  | Request ID |
| `--category` |  |  | Filter by category: APP_USAGE, APP_STORE_ENGAGEMENT, COMMERCE, FRAMEWORK_USAGE, PERFORMANCE |

### asc analytics-reports instances

List report instances for a report

```
asc analytics-reports instances --report-id <report-id> [--granularity <granularity>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--report-id` | yes |  | Report ID |
| `--granularity` |  |  | Filter by granularity: DAILY, WEEKLY, MONTHLY |

### asc analytics-reports segments

List download segments for a report instance

```
asc analytics-reports segments --instance-id <instance-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--instance-id` | yes |  | Instance ID |

## asc reviews

Manage customer reviews

### asc reviews list

List customer reviews for an app

```
asc reviews list --app-id <app-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID |

### asc reviews get

Get a customer review

```
asc reviews get --review-id <review-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--review-id` | yes |  | Review ID |

## asc review-responses

Manage customer review responses

### asc review-responses get

Get the response to a customer review

```
asc review-responses get --review-id <review-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--review-id` | yes |  | Review ID |

### asc review-responses create

Create a response to a customer review

```
asc review-responses create --review-id <review-id> --response-body <response-body>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--review-id` | yes |  | Review ID |
| `--response-body` | yes |  | Response body text |

### asc review-responses delete

Delete a response to a customer review

```
asc review-responses delete --response-id <response-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--response-id` | yes |  | Response ID |

## asc perf-metrics

Download power and performance metrics for apps and builds

### asc perf-metrics list

List performance metrics for an app or build

```
asc perf-metrics list [--app-id <app-id>] [--build-id <build-id>] [--metric-type <metric-type>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` |  |  | App ID (mutually exclusive with --build-id) |
| `--build-id` |  |  | Build ID (mutually exclusive with --app-id) |
| `--metric-type` |  |  | Filter by metric type: HANG, LAUNCH, MEMORY, DISK, BATTERY, TERMINATION, ANIMATION, STORAGE |

## asc diagnostics

List diagnostic signatures for a build

### asc diagnostics list

List diagnostic signatures for a build

```
asc diagnostics list --build-id <build-id> [--diagnostic-type <diagnostic-type>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--build-id` | yes |  | Build ID |
| `--diagnostic-type` |  |  | Filter by diagnostic type: DISK_WRITES, HANGS, LAUNCHES |

## asc diagnostic-logs

List diagnostic logs for a signature

### asc diagnostic-logs list

List diagnostic logs for a signature

```
asc diagnostic-logs list --signature-id <signature-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--signature-id` | yes |  | Diagnostic signature ID |

## asc beta-review

Manage beta app review submissions and details

### asc beta-review submissions list

List beta app review submissions for a build

```
asc beta-review submissions list --build-id <build-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--build-id` | yes |  | Build ID to filter submissions |

### asc beta-review submissions create

Submit a build for beta app review

```
asc beta-review submissions create --build-id <build-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--build-id` | yes |  | Build ID to submit for beta review |

### asc beta-review submissions get

Get a specific beta app review submission

```
asc beta-review submissions get --submission-id <submission-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--submission-id` | yes |  | Beta app review submission ID |

### asc beta-review detail get

Get beta app review contact details for an app

```
asc beta-review detail get --app-id <app-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID |

### asc beta-review detail update

Update beta app review contact details

```
asc beta-review detail update --detail-id <detail-id> [--contact-first-name <contact-first-name>] [--contact-last-name <contact-last-name>] [--contact-phone <contact-phone>] [--contact-email <contact-email>] [--demo-account-name <demo-account-name>] [--demo-account-password <demo-account-password>] [--demo-account-required] [--notes <notes>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--detail-id` | yes |  | Beta app review detail ID |
| `--contact-first-name` |  |  | Contact first name |
| `--contact-last-name` |  |  | Contact last name |
| `--contact-phone` |  |  | Contact phone number |
| `--contact-email` |  |  | Contact email address |
| `--demo-account-name` |  |  | Demo account username |
| `--demo-account-password` |  |  | Demo account password |
| `--demo-account-required` |  |  | Demo account is required |
| `--notes` |  |  | Review notes |

## asc beta-app-localizations

Manage TestFlight beta app descriptions and per-locale metadata

### asc beta-app-localizations list

List beta app localizations for an app

```
asc beta-app-localizations list --app-id <app-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID to list beta localizations for |

### asc beta-app-localizations get

Get a single beta app localization by ID

```
asc beta-app-localizations get --localization-id <localization-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--localization-id` | yes |  | Beta app localization ID |

### asc beta-app-localizations create

Create a beta app localization for an app

```
asc beta-app-localizations create --app-id <app-id> --locale <locale> [--description <description>] [--feedback-email <feedback-email>] [--marketing-url <marketing-url>] [--privacy-policy-url <privacy-policy-url>] [--tv-os-privacy-policy <tv-os-privacy-policy>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID to create the beta localization for |
| `--locale` | yes |  | Locale code (e.g. en-US, zh-Hans) |
| `--description` |  |  | Beta app description shown to TestFlight testers |
| `--feedback-email` |  |  | Tester feedback email |
| `--marketing-url` |  |  | Marketing URL |
| `--privacy-policy-url` |  |  | Privacy policy URL |
| `--tv-os-privacy-policy` |  |  | tvOS-specific privacy policy text |

### asc beta-app-localizations update

Update an existing beta app localization

```
asc beta-app-localizations update --localization-id <localization-id> [--description <description>] [--feedback-email <feedback-email>] [--marketing-url <marketing-url>] [--privacy-policy-url <privacy-policy-url>] [--tv-os-privacy-policy <tv-os-privacy-policy>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--localization-id` | yes |  | Beta app localization ID |
| `--description` |  |  | Beta app description shown to TestFlight testers |
| `--feedback-email` |  |  | Tester feedback email |
| `--marketing-url` |  |  | Marketing URL |
| `--privacy-policy-url` |  |  | Privacy policy URL |
| `--tv-os-privacy-policy` |  |  | tvOS-specific privacy policy text |

### asc beta-app-localizations delete

Delete a beta app localization

```
asc beta-app-localizations delete --localization-id <localization-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--localization-id` | yes |  | Beta app localization ID |

## asc review-submissions

Build, submit and inspect App Store review submissions (app versions plus IAP and subscription versions)

### asc review-submissions list

List App Store review submissions for an app

```
asc review-submissions list --app-id <app-id> [--state <state>] [--limit <limit>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID to filter submissions |
| `--state` |  |  | Comma-separated states (e.g. WAITING_FOR_REVIEW,IN_REVIEW,READY_FOR_REVIEW) |
| `--limit` |  |  | Maximum number of submissions to return |

### asc review-submissions get

Get a single App Store review submission by id (state + affordances to drill into items)

```
asc review-submissions get --submission-id <submission-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--submission-id` | yes |  | Submission ID to fetch |

### asc review-submissions create

Open a draft review submission for an app (reuses the open draft if there is one)

```
asc review-submissions create --app-id <app-id> [--platform <platform>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID |
| `--platform` |  | `ios` | Platform: ios, macos, tvos, visionos |

### asc review-submissions submit

Send a draft review submission, with all its items, to App Review

```
asc review-submissions submit --submission-id <submission-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--submission-id` | yes |  | Review submission ID |

### asc review-submissions items list

List items in a review submission; filter by state to surface rejected items

```
asc review-submissions items list --submission-id <submission-id> [--state <state>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--submission-id` | yes |  | Submission ID whose items to list |
| `--state` |  |  | Filter by item state (e.g. REJECTED, APPROVED, READY_FOR_REVIEW) |

### asc review-submissions items add

Add an app version, or an IAP, subscription or subscription group version, to a draft review submission

```
asc review-submissions items add --submission-id <submission-id> [--version-id <version-id>] [--iap-version-id <iap-version-id>] [--subscription-version-id <subscription-version-id>] [--subscription-group-version-id <subscription-group-version-id>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--submission-id` | yes |  | Review submission ID |
| `--version-id` |  |  | App Store version ID |
| `--iap-version-id` |  |  | In-app purchase version ID (from `asc iap versions list`) |
| `--subscription-version-id` |  |  | Subscription version ID (from `asc subscriptions versions list`) |
| `--subscription-group-version-id` |  |  | Subscription group version ID (from `asc subscription-groups versions list`) |

### asc review-submissions items remove

Remove an item from a draft review submission

```
asc review-submissions items remove --item-id <item-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--item-id` | yes |  | Review submission item ID |

## asc app-availability

Manage app territory availability

### asc app-availability get

Get territory availability for an app with per-territory status

```
asc app-availability get --app-id <app-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID to get availability for |

### asc app-availability create

Set up where an app is available (App Store Connect's "Set Up Availability")

```
asc app-availability create --app-id <app-id> [--territory <territory> …] [--all-territories] [--available-in-new-territories]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--app-id` | yes |  | App ID |
| `--territory` |  |  | Territory to make the app available in (e.g. USA). Repeat for several. |
| `--all-territories` |  |  | Make the app available in every territory Apple sells in (`asc territories list`) |
| `--available-in-new-territories` |  |  | Automatically make the app available in territories Apple adds later |

## asc iap-availability

Manage in-app purchase territory availability

### asc iap-availability get

Get territory availability for an in-app purchase

```
asc iap-availability get --iap-id <iap-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--iap-id` | yes |  | IAP ID to get availability for |

### asc iap-availability create

Create territory availability for an in-app purchase

```
asc iap-availability create --iap-id <iap-id> [--available-in-new-territories] [--territory <territory> …]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--iap-id` | yes |  | IAP ID to set availability for |
| `--available-in-new-territories` |  |  | Automatically make available in new territories Apple adds |
| `--territory` |  |  | Territory ID to include (e.g. USA, CHN, JPN). Repeat for multiple territories. |

## asc subscription-availability

Manage subscription territory availability

### asc subscription-availability get

Get territory availability for a subscription

```
asc subscription-availability get --subscription-id <subscription-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID to get availability for |

### asc subscription-availability create

Create territory availability for a subscription

```
asc subscription-availability create --subscription-id <subscription-id> [--available-in-new-territories] [--territory <territory> …]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--subscription-id` | yes |  | Subscription ID to set availability for |
| `--available-in-new-territories` |  |  | Automatically make available in new territories Apple adds |
| `--territory` |  |  | Territory ID to include (e.g. USA, CHN, JPN). Repeat for multiple territories. |

## asc territories

List available App Store territories

### asc territories list

List all available App Store territories

```
asc territories list
```

## asc web-server

Start the local API server for ASC web apps

```
asc web-server [--port <port>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--port` |  | `8420` | Port to listen on (default: 8420) |

## asc iris

App Store Connect private API (cookie-based auth)

### asc iris status

Check iris cookie session status

```
asc iris status
```

### asc iris apps list

List all apps

```
asc iris apps list
```

### asc iris apps create

Create a new app

```
asc iris apps create --name <name> --bundle-id <bundle-id> --sku <sku> [--primary-locale <primary-locale>] [--platforms <platforms> …]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--name` | yes |  | App name |
| `--bundle-id` | yes |  | Bundle identifier (e.g. com.example.app) |
| `--sku` | yes |  | SKU identifier |
| `--primary-locale` |  | `en-US` | Primary locale (e.g. en-US) |
| `--platforms` |  | `IOS` | Platforms (e.g. IOS, MAC_OS) |

### asc iris iap-submissions create

Submit an IAP for review via iris (attaches to next app version by default)

```
asc iris iap-submissions create --iap-id <iap-id> [--with-next-version] [--no-with-next-version]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--iap-id` | yes |  | IAP ID to submit for review |
| `--with-next-version` |  | `--with-next-version` | Bind the submission to the next App Store version (Apple-required for first-time IAP submissions) |
| `--no-with-next-version` |  |  | Bind the submission to the next App Store version (Apple-required for first-time IAP submissions) |

### asc iris iap-submissions delete

Remove an iris-queued IAP submission (dequeue from next app version)

```
asc iris iap-submissions delete --submission-id <submission-id>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--submission-id` | yes |  | Submission ID (same value as the parent IAP id for iris-queued submissions) |

### asc iris resolution-center get

Read Resolution Center messages and rejection reasons for a review submission

```
asc iris resolution-center get --submission-id <submission-id> [--plain-text] [--out <out>]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--submission-id` | yes |  | Review submission ID (from `asc review-submissions list`) |
| `--plain-text` |  |  | Convert HTML message bodies to plain text |
| `--out` |  |  | Directory to download attachments into (created if missing) |

### asc iris auth login

Sign in with Apple ID + password via SRP. Prompts for the 2FA code separately.

```
asc iris auth login --apple-id <apple-id> [--password <password>] [--interactive]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--apple-id` | yes |  | Apple ID email |
| `--password` |  |  | Apple ID password (omit to be prompted on stdin) |
| `--interactive` |  |  | Single-process: prompt for 2FA code on stdin instead of writing pending state |

### asc iris auth verify-code

Submit the 2FA code from a pending `asc iris auth login`

```
asc iris auth verify-code <code>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `<code>` | yes |  | 6-digit 2FA code |

### asc iris auth logout

Clear the saved iris session (does not revoke cookies on Apple's side — they expire naturally)

```
asc iris auth logout
```

## asc simulators

Manage local iOS simulators

### asc simulators list

List available iOS simulators

```
asc simulators list [--booted]
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--booted` |  |  | Show only booted simulators |

### asc simulators boot

Boot a simulator

```
asc simulators boot --udid <udid>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--udid` | yes |  | Simulator UDID |

### asc simulators shutdown

Shutdown a simulator

```
asc simulators shutdown --udid <udid>
```

| Flag | Required | Default | Description |
|---|---|---|---|
| `--udid` | yes |  | Simulator UDID |
